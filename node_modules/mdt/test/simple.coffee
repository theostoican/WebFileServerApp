fs = require 'fs'
path = require 'path'
assert = require 'assert'

mmd = require '../lib/mdt'


before = fs.readFileSync './test/sample.md', 'UTF-8'

after = mmd()(before)

console.log('simple test')
assert.equal before, after, 'simple test NG'