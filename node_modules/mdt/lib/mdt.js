// Generated by CoffeeScript 1.10.0
(function() {
  var fs, marked, path;

  fs = require('fs');

  path = require('path');

  marked = require('marked');

  module.exports = function(render) {
    var key, renderer, value;
    marked.Lexer.prototype.token = require('../lib/token');
    marked.Parser.prototype.tok = require('../lib/tok');
    renderer = require('../lib/renderer');
    for (key in renderer) {
      value = renderer[key];
      marked.Renderer.prototype[key] = value;
    }
    if ('[object Object]' === Object.prototype.toString.call(render)) {
      for (key in render) {
        value = render[key];
        marked.Renderer.prototype[key] = value;
      }
    }
    return marked;
  };

}).call(this);
