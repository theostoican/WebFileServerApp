module.exports = ()->
  switch (this.token.type)
    when 'space'
      # console.log 'space'
      this.renderer.space this.token.content

    when 'hr'
      # console.log 'hr'
      this.renderer.hr this.token.content

    when 'heading'
      # console.log 'heading'
      this.renderer.heading this.token.content

    when 'code'
      # console.log 'code'
      this.renderer.code this.token.content

    when 'fences'
      # console.log 'code'
      this.renderer.fences this.token.content

    when 'table'
      # console.log 'table'
      this.renderer.table this.token.content

    when 'lheading'
      # console.log 'table'
      this.renderer.lheading this.token.content

    when 'blockquote'
      # console.log 'blockquote'
      this.renderer.blockquote this.token.content

    when 'list'
      # console.log 'list'
      this.renderer.list this.token.content

    when 'def'
      # console.log 'def'
      this.renderer.list this.token.content

    when 'html'
      # console.log 'html'
      this.renderer.html this.token.content

    when 'paragraph'
      # console.log 'paragraph'
      this.renderer.paragraph this.token.content

    when 'text'
      # console.log 'text'
      this.renderer.paragraph this.token.content